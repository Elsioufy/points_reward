#all
  #content
    .container
      .body
        %h2
          Hello #{current_user.name}

        = link_to t('invitation.invite_new_user'), new_invitation_path, class: 'btn btn-primary', id: 'invite_new_user'